<div class="container">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <%= @message.description %>
    <div class="card-body bg-white p-3">

      <%= simple_form_for([@message, @receive_message]) do |f| %>

        <%= f.association :receive, label: "receive" %>
        <%= f.submit "Create msg", class: "btn btn-primary" %>
      <% end %>
        <%= link_to "Add New", new_receife_path, method: :post, class: "btn btn-success" %>
        <%= link_to "+", new_receife_path, method: :post, class: "btn btn-success" %>
      </div>
    </div>
  </div>
</div>
